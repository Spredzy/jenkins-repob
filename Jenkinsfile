pipeline {

    agent none

    stages {
        stage('Scenario') {
            steps {
                script {
                    echo 'hello world'
                    /* jobs = load('build-scenarios.groovy') */
                    /* parallel jobs */
                }
            }
        }
    }

}
